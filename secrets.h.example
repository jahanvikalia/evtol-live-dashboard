// secrets.h.example — copy this to secrets.h and fill in your own values.
// secrets.h is gitignored and will NOT be uploaded to GitHub.
#define WIFI_SSID     "YourNetworkName"
#define WIFI_PASSWORD "YourPassword"
